#include<bits/stdc++.h>
using namespace std;
void func(int n)
{ int a[n];
    for(int i=0;i<n;i++)
{  cin>>a[i]; }

 if (n == 1 || n == 2) { cout<<-1;  return ; }
  int element = a[0], maxx = a[0], bit = -1;
  int flag=0;
    int idx = -1;
  for (int i = 1; i < (n - 1);i++) {
 
   if (a[i] < maxx ) {
       
              if(a[i]<element)
              {bit = 0;
             flag=0;  }
                 }
 
        else {
            if(flag==0)
{
              element = a[i];
                idx = i;
               maxx = a[i];
              
                bit = 1;
                flag=1;
            }
            if(a[i]>maxx)
            {
                maxx=a[i];
            }
           
        }
    } 
    if (element <= a[n - 1] && bit == 1) {
        cout<<a[idx];
       return ;
    }
    else {
   
        cout<<-1; return ;
    } 
}
int main() {
	int t,n;
	cin>>t;
	for(int i=0;i<t;i++)
	{
	    cin>>n;
func(n);

	    cout<<endl;
	}
	return 0;
}
